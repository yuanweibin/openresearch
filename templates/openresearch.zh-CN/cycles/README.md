# 研究 Cycle

使用两位数、单调递增的序号创建 Cycle 目录，例如 `01-first-chain` 和 `02-decoder-capacity`。序号表示创建顺序，不表示优先级或完成顺序。

每个 Cycle 应减少一个主要科学不确定性，并包含 `question.md`、`experiment.md`、`deliverables.md`、`tasks.md`、`status.md` 和 `decisions.md`。执行过程中将影响决策的产物发布到 `results/`。
